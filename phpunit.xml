<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="./vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true">
    <testsuites>
        <testsuite name="Unit">
            <directory suffix="Test.php">./tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory suffix="Test.php">./tests/Feature</directory>
        </testsuite>
    </testsuites>
    <!--
        Code coverage: exclude declarative Filament Resources/Widgets and thin CmsPlugin entry;
        app/Filament/Utils stays included (module-specific traits/helpers — cover with tests).

        Subset runs: pest with coverage and exactly=100 on one file still measures line coverage across
        ALL paths in include below, so the total stays low unless you run the full suite. For a single
        file use composer test:pest with a path (no global coverage gate), or run composer test:unit
        when you need exactly 100 percent on the whole module.
    -->
    <source>
        <include>
            <directory suffix=".php">./app</directory>
        </include>
        <exclude>
            <directory>./app/Filament/Resources</directory>
            <directory>./app/Filament/Widgets</directory>
            <file>./app/Filament/CmsPlugin.php</file>
            <directory>./app/Analytics</directory>
            <directory>./app/Providers</directory>
        </exclude>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="SCOUT_DRIVER" value="collection"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit> 